**项目设计文档：Rider-Waite-Smith 单张塔罗牌解说网站**

**1. 引言 (Introduction)**

* **项目名称:** RWS 单张塔罗牌解说网站
* **目标:** 构建一个简单、直观的网站，用户可以通过模拟抽牌过程，获取一张 Rider-Waite-Smith 塔罗牌，并查看该牌在正位或逆位状态下的详细解说，为寻求快速指引的用户提供参考。
* **技术栈:** 后端使用 Node.js，数据存储使用 JSON 文件，前端使用 HTML, CSS, JavaScript (可考虑轻量级框架)。

**2. 项目范围 (Scope)**

* **包含 (In Scope):**
    * 使用 Rider-Waite-Smith (RWS) 塔罗牌组的全部 78 张牌。
    * 提供单张牌的抽取功能。
    * 区分牌的正位和逆位（各占约 50% 概率）。
    * 提供详细的牌面解说，内容包括：故事背景、象征意义、针对感情情境的解读、针对事业情境的解读、针对学业情境的解读，以及一个整体概括。
    * 展示抽到的牌面图片、牌名及正逆位状态。
    * 实现模拟洗牌、切牌、翻牌的动画效果。
    * 用户可以查看抽到的牌和解说，但不可进行其他操作（如修改、保存、分享）。
    * 网站界面简洁，用户流程清晰，面向寻求快速指引的群体。
* **不包含 (Out of Scope):**
    * 其他塔罗牌组。
    * 多种牌阵（如三张牌、凯尔特十字等）。
    * 用户输入问题或意图。
    * 用户账户系统、抽牌历史记录保存。
    * 解说内容的个性化、根据特定问题调整解说。
    * 社区功能、塔罗知识库、每日一牌等附加功能。
    * 商业化付费功能。

**3. 用户流程 (User Flow)**

1.  用户访问网站首页。
2.  用户在页面上看到开始抽牌的入口（例如一个按钮或一个牌堆图像）。
3.  用户点击开始抽牌的入口。
4.  网站播放模拟洗牌、切牌、翻牌的动画效果。
5.  动画结束后，页面中央显示抽到的那一张塔罗牌的正面图像，同时显示牌名和是正位还是逆位。
6.  在牌面下方，完整地展示该牌在当前正逆位状态下的详细解说内容。
7.  用户阅读解说内容。
8.  用户可以再次点击开始抽牌的入口，重复上述过程。

**4. 技术架构 (Technical Architecture)**

* **整体架构:** Client (Browser) <-> Frontend (HTML/CSS/JavaScript) <-> Backend (Node.js/Express) <-> Data Storage (JSON Files)
* **前端 (Frontend):**
    * **技术:** HTML (结构), CSS (样式), JavaScript (交互, 动画, API 调用)。可以考虑使用 Vue 或 React 的轻量级版本或只用原生 JS。
    * **模块/组件:**
        * `Header` (可选, 网站标题)
        * `TarotArea`: 显示牌堆、执行抽牌动画、显示最终抽到的牌面。
        * `InterpretationArea`: 显示抽到的牌名、正逆位状态以及详细解说文本。
        * `ControlArea`: 包含触发抽牌操作的按钮。
    * **功能:**
        * 监听用户点击事件，触发抽牌流程。
        * 调用后端 API 获取抽牌结果（包括牌信息和解说文本）。
        * 控制并展示洗牌、切牌、翻牌的动画效果。
        * 根据后端返回的数据，更新 `TarotArea` 和 `InterpretationArea` 的内容。
* **后端 (Backend):**
    * **技术:** Node.js (运行时), Express.js (Web 框架)。
    * **模块/功能:**
        * 读取并加载 JSON 数据（牌面信息和解说）。建议在应用启动时加载一次到内存中，避免每次请求都读文件。
        * 提供一个 API 端点用于抽牌。
    * **API 端点:**
        * `POST /api/draw_card`:
            * **请求:** 无请求体。
            * **处理:**
                1.  从已加载的 78 张牌数据中，随机抽取一张牌。
                2.  随机确定该牌是正位还是逆位（例如，使用 `Math.random()` 生成 0 或 1）。
                3.  根据抽到的牌的名称和正逆位状态，从已加载的解说数据中查找对应的详细解说文本。
                4.  将抽到的牌的信息（牌名、正逆位、图片文件名/URL）和查找到的解说文本一起组合成响应数据。
            * **响应:** JSON 格式，包含抽到的牌的详细信息和对应的解说文本。
                ```json
                {
                  "card": {
                    "name": "愚者",
                    "english_name": "The Fool",
                    "arcana": "大阿卡那",
                    "is_reversed": false, // true 表示逆位
                    "image_filename": "00_The_Fool.jpg" // 或图片的 URL
                  },
                  "interpretation": {
                    "story_background": "...",
                    "symbolism": "...",
                    "love": "...",
                    "career": "...",
                    "study": "...",
                    "overall": "..."
                  }
                }
                ```
* **数据存储 (Data Storage):**
    * **文件格式:** JSON 文件。
    * **数据结构:**
        * `cards.json`: 存储 78 张牌的基本信息数组。
            ```json
            [
              {
                "id": 0,
                "name": "愚者",
                "english_name": "The Fool",
                "arcana": "大阿卡那",
                "suit": null, // 大阿卡那无花色
                "rank": 0,
                "image_filename": "00_The_Fool.jpg"
              },
              {
                "id": 1,
                "name": "魔术师",
                "english_name": "The Magician",
                "arcana": "大阿卡那",
                "suit": null,
                "rank": 1,
                "image_filename": "01_The_Magician.jpg"
              },
              // ... 其他 76 张牌
              {
                "id": 77,
                "name": "星币王牌",
                "english_name": "Ace of Pentacles",
                "arcana": "小阿卡那",
                "suit": "星币",
                "rank": 1, // 或表示 Ace
                "image_filename": "56_Ace_of_Pentacles.jpg"
              }
            ]
            ```
        * `interpretations.json`: 存储所有牌的正位和逆位解说文本的对象。以牌的唯一标识（如 `english_name` 或 `id`）作为键。
            ```json
            {
              "The Fool": {
                "upright": {
                  "story_background": "...",
                  "symbolism": "...",
                  "love": "...",
                  "career": "...",
                  "study": "...",
                  "overall": "..."
                },
                "reversed": {
                  "story_background": "...",
                  "symbolism": "...",
                  "love": "...",
                  "career": "...",
                  "study": "...",
                  "overall": "..."
                }
              },
              "The Magician": {
                // ... 解说内容
              },
              // ... 所有 78 张牌的解说
            }
            ```
    * **图片文件:** 将塔罗牌图片文件（例如 `.jpg`, `.png`）存放在前端可访问的静态文件夹中（例如 `/public/images/tarot/`）。`image_filename` 字段应对应这些文件的名称。

**5. 功能详细设计 (Feature Details)**

* **单张牌抽取:** 后端实现从 78 个选项中等概率随机抽取一个，并以约 50% 的概率决定正逆位。
* **动画效果:**
    * 前端需要一个包含所有牌背面图像的区域作为牌堆。
    * 点击触发后，通过 CSS 过渡或 JavaScript 动画库（如 GreenSock Animation Platform - GSAP）实现以下序列动画：
        1.  牌堆“洗牌”效果（例如，快速移动、旋转）。
        2.  模拟“切牌”，从牌堆中抽取一部分移动到旁边。
        3.  从剩余的牌堆中选取一张（逻辑上已由后端确定是哪张），移动到中央。
        4.  翻牌动画（例如，沿 Y 轴旋转 180 度，背面翻转为正面）。
    * 动画播放期间，前端可以显示“正在抽牌...”等提示文字。
* **解说展示:**
    * 后端返回解说数据后，前端将解说文本分别填充到对应的 HTML 元素中（例如，每个部分一个标题和一个段落）。
    * 确保解说区域有良好的排版和可读性。

**6. 非功能性需求 (Non-Functional Requirements)**

* **可用性:** 界面简洁直观，用户无需学习即可使用。
* **性能:** 页面加载速度快，动画流畅。数据加载（从 JSON 文件）应在服务器启动时完成，避免延迟。
* **响应式设计:** 网站应能在不同大小的屏幕上（桌面、平板、手机）正常显示和使用。
* **稳定性:** 后端 API 应能稳定响应请求。
* **可维护性:** 代码结构清晰，易于理解和修改。

**7. 未来潜在扩展 (Potential Future Extensions)**

* 增加更多牌阵。
* 增加用户输入问题的功能（即使初期不影响解说，也可以显示出来）。
* 添加塔罗牌知识库或牌面图鉴页面。
* 考虑迁移到数据库以支持更复杂的数据操作和用户功能。。